
#TODO: check on exercise of prof on gitlab if all libraries are correct before pushing on git

add_library(messages SHARED
    base_sensor_message.cpp
    odometry_message.cpp
    pose_message.cpp
    )